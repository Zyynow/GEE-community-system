<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jxufe.dao.AdminDao">
    <select id="findByUsernameAndPassword" resultType="com.jxufe.entity.Admin">
        select * from t_admin where username = #{username} and password = #{password}
    </select>

    <insert id="saveUser" parameterType="com.jxufe.entity.Admin">
        insert into t_admin(username, password, email, description, avatar)
        values(#{username}, #{password}, #{email}, #{description}, #{avatar})
    </insert>

    <select id="findByUsername" resultType="com.jxufe.entity.Admin">
        select * from t_admin where username = #{username}
    </select>
</mapper>